#include <iostream>

int main() {
    // say we got an array with 10 elements and we wanted to 
    // print them all out
    int numElements = 10;
    for (int i = 0; i <= numElements; ++i) {
        std::cout << i << " ";
    }

    return 0;
}